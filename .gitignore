# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Tooling / agent output
.playwright-mcp
.impeccable
.prototools

# TinaCMS generated output (also covered by tina/.gitignore)
tina/__generated__

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Output of 'npm pack'
*.tgz

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Yarn Integrity file
.yarn-integrity

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Astro
.astro

# Package Manager Lock Files (pnpm-lock.yaml is committed on purpose)
package-lock.json
yarn.lock
.yarn/cache
bun.lockb
deno.lock

# Migration working data. Re-creatable with `node migration/scripts/1-extract.mjs`
# and the Playwright capture; 64 MB of raw WordPress JSON and reference
# screenshots is not worth carrying in history.
migration/raw
migration/design/shots

# builds
.vercel
.netlify
.wrangler
dist

node_modules